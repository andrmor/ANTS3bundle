var dir = "/home/andr/.config/ants3/TmpOutput"
var ar = core.loadNumericArray(dir + "/SensorSignals.txt")

histName = "h"
hist.new1D(histName, 100, 700, 1300)
hist.fillArr(histName, ar)
hist.draw(histName, "hist")
